<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.EmployeeMapper">
  <sql id="query_employee">
    select id,
           name,
           username,
           password,
           phone,
           sex,
           id_number,
           status,
           create_time,
           update_time,
           create_user,
           update_user
    from employee
  </sql>
  <select id="getAllEmployee" resultType="com.sky.entity.Employee">
    <include refid="query_employee" />
    <where>
      <if test="name != null">
        name LIKE CONCAT("%",#{name},"%")
      </if>
    </where>
  </select>
  <insert id="addEmployee">
    INSERT INTO employee (id, NAME, id_number, sex, username, phone, PASSWORD, create_user, update_user) VALUES (NULL, #{name}, #{idNumber}, #{sex}, #{username}, #{phone}, MD5("123456"), 1, 1);
  </insert>
  <select id="getEmployeeById" resultType="com.sky.entity.Employee">
    <include refid="query_employee" />
    where id = #{id}
  </select>
  <update id="updateEmployee">
    update employee
    <set>
      <if test="idNumber != null">
        id_number = #{idNumber},
      </if>
      <if test="name != null">
        name = #{name},
      </if>
      <if test="phone != null">
        phone = #{phone},
      </if>
      <if test="sex != null">
        sex = #{sex},
      </if>
      <if test="username != null">
        username = #{username}
      </if>
    </set>
    <where>
      id = #{id}
    </where>
  </update>

</mapper>
